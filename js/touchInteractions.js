// Only initialize hover effects for devices with true hover capability
export function initTouchInteractions() {
  const container = document.querySelector('.shoe-image-container');
  if (!container) return;

  // We'll handle all interactions through CSS media queries for hover capability
  // No JavaScript touch handling needed
}
